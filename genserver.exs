# call -> sincrono. Server é obrigado a enviar uma resposta para cada request.
# enquanto o server processa o respose, o client fica aguardando

# cast -> assincrono. Server não envia devolta um response e portanto,
# o cliente não espera por um.